package enums;

/**
 * Game States
 * @author
 */
enum GameState 
{
	Initalize;	// Draw cards etc
	World;		// Pick where to go
	Event;		// Opening and active event card
	Trade;		// Trading
	Draw;		// Draw Card
}